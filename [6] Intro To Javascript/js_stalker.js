// Input the first name of the user
let firstName = window.prompt("Pleaser enter your first name");

// Input the last name of the user
let lastName = window.prompt("Please enter your last name");

// Input the age of the user
let user_age = window.prompt("Please enter your age");

console.log("Hello, " + firstName + " " + lastName);
console.log("You entered your age as: " + user_age);